import receiptManualTaxDeliverSlice from '@/store-redux/slices/receipt-manual/tax-deliver/slice';

export const receiptManualTaxDeliver = {
  // export ไปให้ file index.tsx ในfolder (reducer)
  receiptManualTaxDeliverSlice,
};
